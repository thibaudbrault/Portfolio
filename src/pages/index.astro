---
import Layout from '@/layouts/Layout.astro';
import { Hero, Separator, Experiences, Pokeref, Portfolio, Bio, UnitConverter } from '@/components';

const title = "Thibaud Brault"
const description = "Thibaud Brault, développeur front-end"
---

<Layout {title} {description}>
	<div class="mx-auto max-w-7xl">
		<Hero />
		<Separator className='mx-auto mb-24 w-2/3 bg-stone-200' />
		<Experiences client:visible />
		<Separator className='mx-auto mb-24 w-2/3 bg-stone-200' />
		<section id="projects">
			<Pokeref client:visible />
			<UnitConverter client:visible />
			<Portfolio client:visible />
		</section>
		<Separator className='mx-auto mb-24 w-2/3 bg-stone-200' />
		<Bio client:visible />
	</div>
</Layout>